--- a/net/minecraft/world/entity/SpawnPlacements.java
+++ b/net/minecraft/world/entity/SpawnPlacements.java
@@ -75,7 +79,13 @@
         EntityType<T> type, ServerLevelAccessor level, EntitySpawnReason spawnReason, BlockPos pos, RandomSource random
     ) {
         SpawnPlacements.Data data = DATA_BY_TYPE.get(type);
-        return data == null || ((SpawnPredicate<T>)data.predicate).test(type, level, spawnReason, pos, random);
+        boolean vanillaResult = data == null || data.predicate.test((EntityType)type, level, spawnReason, pos, random);
+        return net.neoforged.neoforge.event.EventHooks.checkSpawnPlacements(type, level, spawnReason, pos, random, vanillaResult);
+    }
+
+    // Neo: Added to allow for checking if an entity has a spawn placement
+    public static boolean hasPlacement(EntityType<?> type) {
+        return DATA_BY_TYPE.containsKey(type);
     }
 
     static {
